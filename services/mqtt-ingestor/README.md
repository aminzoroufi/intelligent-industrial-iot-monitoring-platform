# MQTT ingestor path

The importable Python package is `services/mqtt_ingestor` because hyphens are
not valid Python package identifiers. This directory preserves the public
monorepo map and points to the independently runnable worker implementation.

